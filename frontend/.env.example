# Desenvolvimento: deixe vazio — o Vite faz proxy de /api para localhost:3000 (vite.config.js).
VITE_API_URL=

# Produção (ex.: Vercel): obrigatório — URL completa da API, SEM barra no fim.
# VITE_API_URL=https://xxxx.execute-api.us-east-2.amazonaws.com/dev
