{
  "name": "challenge-wach-fullstack",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "setup:env": "npm run setup:env -w backend",
    "migrate": "npm run migrate -w backend",
    "test": "npm test -w backend"
  }
}
