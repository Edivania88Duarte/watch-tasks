DATABASE_URL=postgresql://postgres:postgres@localhost:5432/watch_tasks
JWT_SECRET=altere-em-producao
JWT_EXPIRES_IN=7d
PORT=3000
PG_POOL_MAX=5
# URL base da API após deploy (sem barra no fim). Copie do output de `serverless deploy` (endpoints).
# Ex.: https://xxxxxxxxxx.execute-api.us-east-2.amazonaws.com/dev
PUBLIC_API_URL=
